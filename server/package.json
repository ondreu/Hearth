{
	"name": "hearth-gallery-server",
	"private": true,
	"version": "1.0.0",
	"description": "A self-hosted gallery for Hearth dashboard packages.",
	"type": "module",
	"license": "MIT",
	"engines": {
		"node": ">=22.5"
	},
	"scripts": {
		"build": "node esbuild.config.mjs",
		"start": "node dist/index.js",
		"dev": "node esbuild.config.mjs && node --watch dist/index.js",
		"typecheck": "tsc --noEmit -p tsconfig.json",
		"smoke": "node build-test.mjs && node dist/test/fixtures.js > dist/test/fixtures.json && node dist/test/smoke.js dist/test/fixtures.json"
	}
}
